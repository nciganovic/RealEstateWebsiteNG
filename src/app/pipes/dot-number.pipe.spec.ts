import { DotNumberPipe } from './dot-number.pipe';

describe('DotNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new DotNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
